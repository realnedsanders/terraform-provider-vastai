---
phase: 06-documentation-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - LICENSE
  - .github/CONTRIBUTING.md
  - examples/workflows/gpu_instance/main.tf
  - examples/workflows/serverless_endpoint/main.tf
  - examples/workflows/team_management/main.tf
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "README has a comprehensive table listing all 17 resources and 11 data sources"
    - "GPU instance workflow example shows end-to-end: search offers, create template, add SSH key, launch instance"
    - "Serverless workflow example shows create endpoint, add worker group, query status"
    - "Team management example shows create team, define roles, invite members"
    - "CHANGELOG.md exists with initial v0.1.0 entry"
    - "LICENSE file contains MPL-2.0 full text"
    - "CONTRIBUTING.md covers dev setup, testing, and PR process"
  artifacts:
    - path: "README.md"
      provides: "Provider overview with resource/data source table"
      contains: "vastai_instance"
    - path: "examples/workflows/gpu_instance/main.tf"
      provides: "Hero workflow example for GPU instance creation"
      contains: "vastai_gpu_offers"
    - path: "examples/workflows/serverless_endpoint/main.tf"
      provides: "Serverless inference workflow example"
      contains: "vastai_endpoint"
    - path: "examples/workflows/team_management/main.tf"
      provides: "Team management workflow example"
      contains: "vastai_team"
    - path: "CHANGELOG.md"
      provides: "Release changelog"
      contains: "## 0.1.0"
    - path: "LICENSE"
      provides: "MPL-2.0 license text"
      contains: "Mozilla Public License"
    - path: ".github/CONTRIBUTING.md"
      provides: "Contributing guide"
      contains: "make test"
  key_links:
    - from: "README.md"
      to: "docs/"
      via: "Registry documentation link"
      pattern: "registry.terraform.io"
    - from: "examples/workflows/gpu_instance/main.tf"
      to: "vastai_gpu_offers + vastai_template + vastai_ssh_key + vastai_instance"
      via: "Terraform resource references"
      pattern: "vastai_gpu_offers|vastai_instance"
---

<objective>
Create workflow example configurations, update README with comprehensive resource table, and add release files (CHANGELOG.md, LICENSE, CONTRIBUTING.md).

Purpose: Per D-02, D-03, D-04, the workflow examples demonstrate real use cases users will copy. Per D-06, D-07, D-08, D-09, the repo metadata files are required for registry publishing and open-source contribution.
Output: 3 workflow examples, updated README, CHANGELOG.md, LICENSE (MPL-2.0), .github/CONTRIBUTING.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-release/06-CONTEXT.md
@internal/provider/provider.go
@README.md
@GNUmakefile
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow example configurations and provider docs</name>
  <files>
    examples/workflows/gpu_instance/main.tf,
    examples/workflows/serverless_endpoint/main.tf,
    examples/workflows/team_management/main.tf
  </files>
  <read_first>
    internal/services/instance/resource_instance.go,
    internal/services/offer/data_source_gpu_offers.go,
    internal/services/template/resource_template.go,
    internal/services/sshkey/resource_ssh_key.go,
    internal/services/endpoint/resource_endpoint.go,
    internal/services/workergroup/resource_worker_group.go,
    internal/services/endpoint/data_source_endpoints.go,
    internal/services/team/resource_team.go,
    internal/services/teamrole/resource_team_role.go,
    internal/services/teammember/resource_team_member.go,
    examples/provider/provider.tf
  </read_first>
  <action>
    **Per D-02 (GPU instance workflow - the "hero" example):** Create `examples/workflows/gpu_instance/main.tf` that demonstrates the most common use case end-to-end:
    1. Configure the provider (reference VASTAI_API_KEY env var)
    2. Search for GPU offers using `vastai_gpu_offers` data source (gpu_name = "RTX_4090", num_gpus = 1, order by dph_total ascending, limit 1)
    3. Create a template using `vastai_template` resource (image = "pytorch/pytorch:latest", env vars, onstart_cmd)
    4. Create an SSH key using `vastai_ssh_key` resource (with a placeholder public key)
    5. Launch an instance using `vastai_instance` resource referencing the offer, template, and SSH key
    6. Add output blocks for instance IP, SSH port, status

    Use realistic values. Read each resource Schema() to determine exact attribute names. Add comments explaining each step. This is the example users will copy first.

    **Per D-03 (Serverless workflow):** Create `examples/workflows/serverless_endpoint/main.tf`:
    1. Configure provider
    2. Create a serverless endpoint with model name, GPU type, and autoscaling config (min_load, target_util, cold_mult, cold_workers, max_workers)
    3. Create a worker group bound to the endpoint with template_hash_id and search parameters
    4. Use `vastai_endpoints` data source to query endpoint status
    5. Add output blocks for endpoint URL and status

    **Per D-04 (Team management):** Create `examples/workflows/team_management/main.tf`:
    1. Configure provider
    2. Create a team using `vastai_team`
    3. Create a custom role using `vastai_team_role` with permissions JSON
    4. Invite a team member using `vastai_team_member` with email and role reference
    5. Add output blocks

    **Per D-02 (Provider configuration docs):** The provider index.md template already exists at `templates/index.md.tmpl`. It covers authentication and basic usage. Verify it documents:
    - `api_key` attribute and VASTAI_API_KEY env var
    - `api_url` attribute and VASTAI_API_URL env var
    - Default endpoint URL
    If missing any of the above, update the template. The retry behavior is internal to the client and not user-configurable, so it does not need documentation in the provider block (but can be noted in a paragraph).
  </action>
  <acceptance_criteria>
    - test -f examples/workflows/gpu_instance/main.tf
    - test -f examples/workflows/serverless_endpoint/main.tf
    - test -f examples/workflows/team_management/main.tf
    - grep "vastai_gpu_offers" examples/workflows/gpu_instance/main.tf
    - grep "vastai_instance" examples/workflows/gpu_instance/main.tf
    - grep "vastai_template" examples/workflows/gpu_instance/main.tf
    - grep "vastai_ssh_key" examples/workflows/gpu_instance/main.tf
    - grep "vastai_endpoint" examples/workflows/serverless_endpoint/main.tf
    - grep "vastai_worker_group" examples/workflows/serverless_endpoint/main.tf
    - grep "vastai_team" examples/workflows/team_management/main.tf
    - grep "vastai_team_role" examples/workflows/team_management/main.tf
    - grep "vastai_team_member" examples/workflows/team_management/main.tf
  </acceptance_criteria>
  <verify>
    <automated>test -f examples/workflows/gpu_instance/main.tf && test -f examples/workflows/serverless_endpoint/main.tf && test -f examples/workflows/team_management/main.tf && grep -c "vastai_" examples/workflows/gpu_instance/main.tf && grep -c "vastai_" examples/workflows/serverless_endpoint/main.tf && grep -c "vastai_" examples/workflows/team_management/main.tf</automated>
  </verify>
  <done>Three workflow examples created. GPU instance example shows full offer-to-instance pipeline. Serverless example shows endpoint + worker group + status query. Team example shows team + role + member invite. All examples use realistic attribute values derived from actual schema.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and create release files (CHANGELOG, LICENSE, CONTRIBUTING)</name>
  <files>
    README.md,
    CHANGELOG.md,
    LICENSE,
    .github/CONTRIBUTING.md
  </files>
  <read_first>
    README.md,
    internal/provider/provider.go,
    GNUmakefile,
    .github/workflows/test.yml,
    .github/workflows/release.yml
  </read_first>
  <action>
    **Per D-06 (README update):** Rewrite README.md to include:
    1. Title: "Terraform Provider for Vast.ai"
    2. Brief description paragraph (GPU compute, storage, networking, serverless, account management via Terraform)
    3. Requirements section (Terraform >= 1.0, Go >= 1.25 for building)
    4. Usage section with provider configuration example (keep existing)
    5. Authentication section (VASTAI_API_KEY env var, provider attribute)
    6. **Resources table** — a markdown table listing ALL 17 resources with:
       | Resource | Description |
       Each resource gets a one-line description.
    7. **Data Sources table** — a markdown table listing ALL 11 data sources with:
       | Data Source | Description |
    8. Building section (git clone, make build)
    9. Development section (make lint, make test, make testacc)
    10. Documentation link to Terraform Registry
    11. License: MPL-2.0

    The resource table must include exactly these 17 resources:
    - `vastai_api_key` - Manages API keys with permission scoping
    - `vastai_cluster` - Manages GPU clusters
    - `vastai_cluster_member` - Manages cluster machine membership
    - `vastai_endpoint` - Manages serverless inference endpoints with autoscaling
    - `vastai_environment_variable` - Manages account environment variables
    - `vastai_instance` - Manages GPU compute instances with full lifecycle
    - `vastai_network_volume` - Manages network-attached persistent volumes
    - `vastai_overlay` - Manages network overlays for secure communication
    - `vastai_overlay_member` - Manages instance membership in overlays
    - `vastai_ssh_key` - Manages SSH keys for instance access
    - `vastai_subaccount` - Manages subaccounts
    - `vastai_template` - Manages instance templates (image, env, startup)
    - `vastai_volume` - Manages local persistent volumes
    - `vastai_worker_group` - Manages serverless worker groups
    - `vastai_team` - Manages teams
    - `vastai_team_role` - Manages team roles with granular permissions
    - `vastai_team_member` - Manages team member invitations

    The data source table must include exactly these 11 data sources:
    - `vastai_audit_logs` - Reads account activity audit logs
    - `vastai_endpoints` - Lists serverless endpoints
    - `vastai_gpu_offers` - Searches available GPU offers with filters
    - `vastai_instance` - Reads a single instance by ID
    - `vastai_instances` - Lists instances with optional filtering
    - `vastai_invoices` - Reads billing invoice history
    - `vastai_network_volume_offers` - Searches network volume offers
    - `vastai_ssh_keys` - Lists SSH keys
    - `vastai_templates` - Searches templates
    - `vastai_user` - Reads current account profile
    - `vastai_volume_offers` - Searches volume offers

    **Per D-07 (CHANGELOG.md):** Create CHANGELOG.md following Keep a Changelog format:
    ```
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ## [0.1.0] - YYYY-MM-DD

    ### Added

    - Initial release of terraform-provider-vastai
    - **Resources:** vastai_instance, vastai_template, vastai_ssh_key, vastai_volume, vastai_network_volume, vastai_endpoint, vastai_worker_group, vastai_api_key, vastai_environment_variable, vastai_team, vastai_team_role, vastai_team_member, vastai_cluster, vastai_cluster_member, vastai_overlay, vastai_overlay_member, vastai_subaccount
    - **Data Sources:** vastai_gpu_offers, vastai_instance, vastai_instances, vastai_templates, vastai_ssh_keys, vastai_endpoints, vastai_volume_offers, vastai_network_volume_offers, vastai_user, vastai_invoices, vastai_audit_logs
    - Provider authentication via API key (env var or provider attribute)
    - Full CRUD lifecycle for all resources with import support
    - Acceptance tests and unit tests
    - CI/CD pipeline with GoReleaser
    ```
    Use today's date (2026-03-28) for the release date.

    **Per D-08 (LICENSE):** Create LICENSE file with the full MPL-2.0 text. Use the exact Mozilla Public License Version 2.0 text from https://www.mozilla.org/en-US/MPL/2.0/ — the standard form used by all HashiCorp-ecosystem Terraform providers. The copyright line should reference the project.

    **Per D-09 (CONTRIBUTING.md):** Create `.github/CONTRIBUTING.md` with sections:
    1. Contributing to terraform-provider-vastai (intro)
    2. Development Setup (Go 1.25+, clone, make build)
    3. Making Changes (create branch, make changes, add tests)
    4. Testing:
       - `make test` for unit tests
       - `make testacc` for acceptance tests (requires VASTAI_API_KEY)
       - `make lint` for linting
    5. Pull Request Process (describe changes, ensure tests pass, follow Go conventions)
    6. Reporting Issues (use GitHub Issues)
    7. Code of Conduct reference (standard GitHub CoC)
  </action>
  <acceptance_criteria>
    - grep "vastai_instance" README.md (resource table has instance)
    - grep "vastai_audit_logs" README.md (resource table has audit_logs data source)
    - grep -c "vastai_" README.md returns at least 28 (17 resources + 11 data sources)
    - test -f CHANGELOG.md
    - grep "0.1.0" CHANGELOG.md
    - test -f LICENSE
    - grep "Mozilla Public License" LICENSE
    - test -f .github/CONTRIBUTING.md
    - grep "make test" .github/CONTRIBUTING.md
    - grep "make testacc" .github/CONTRIBUTING.md
  </acceptance_criteria>
  <verify>
    <automated>grep -c "vastai_" README.md && test -f CHANGELOG.md && grep -c "0.1.0" CHANGELOG.md && test -f LICENSE && grep -c "Mozilla Public License" LICENSE && test -f .github/CONTRIBUTING.md && grep -c "make test" .github/CONTRIBUTING.md</automated>
  </verify>
  <done>README.md has comprehensive resource and data source tables (17 + 11). CHANGELOG.md tracks v0.1.0 initial release. LICENSE contains MPL-2.0 full text. CONTRIBUTING.md covers dev setup, testing, and PR process.</done>
</task>

</tasks>

<verification>
- `test -f examples/workflows/gpu_instance/main.tf` — GPU workflow example exists
- `test -f examples/workflows/serverless_endpoint/main.tf` — Serverless workflow example exists
- `test -f examples/workflows/team_management/main.tf` — Team workflow example exists
- `grep -c "vastai_" README.md` returns >= 28
- `test -f CHANGELOG.md && test -f LICENSE && test -f .github/CONTRIBUTING.md`
- `grep "Mozilla Public License" LICENSE`
</verification>

<success_criteria>
Three workflow examples demonstrate real GPU, serverless, and team use cases. README lists all 17 resources and 11 data sources. CHANGELOG, LICENSE (MPL-2.0), and CONTRIBUTING.md are present for registry publishing and open-source contribution.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-release/06-02-SUMMARY.md`
</output>
