---
phase: 02-core-compute
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - internal/services/instance/resource_instance_acc_test.go
  - internal/services/template/resource_template_acc_test.go
  - internal/services/sshkey/resource_ssh_key_acc_test.go
  - internal/services/offer/data_source_gpu_offers_acc_test.go
  - internal/services/instance/data_source_instance_acc_test.go
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Running TF_ACC=1 go test against instance package exercises create, read, update, import, and destroy of a real Vast.ai instance"
    - "Running TF_ACC=1 go test against template package exercises create, read, update, import, and destroy of a real Vast.ai template"
    - "Running TF_ACC=1 go test against sshkey package exercises create, read, update, import, and destroy of a real SSH key"
    - "Running TF_ACC=1 go test against offer package exercises a live GPU offer search and verifies results have expected fields"
    - "Running TF_ACC=1 go test against instance data sources exercises read of a real instance by ID and list of all instances"
    - "All acceptance test functions are gated behind TF_ACC environment variable"
    - "Acceptance tests use the cheapest available offer to minimize cost"
  artifacts:
    - path: "internal/services/instance/resource_instance_acc_test.go"
      provides: "Acceptance tests for vastai_instance resource and data sources"
      exports: ["TestAccInstance_basic", "TestAccInstance_update", "TestAccInstance_import", "TestAccInstanceDataSource_basic", "TestAccInstancesDataSource_basic"]
    - path: "internal/services/template/resource_template_acc_test.go"
      provides: "Acceptance tests for vastai_template resource and data source"
      exports: ["TestAccTemplate_basic", "TestAccTemplate_update", "TestAccTemplate_import", "TestAccTemplatesDataSource_basic"]
    - path: "internal/services/sshkey/resource_ssh_key_acc_test.go"
      provides: "Acceptance tests for vastai_ssh_key resource and data source"
      exports: ["TestAccSSHKey_basic", "TestAccSSHKey_update", "TestAccSSHKey_import", "TestAccSSHKeysDataSource_basic"]
    - path: "internal/services/offer/data_source_gpu_offers_acc_test.go"
      provides: "Acceptance tests for vastai_gpu_offers data source"
      exports: ["TestAccGpuOffersDataSource_basic", "TestAccGpuOffersDataSource_filtered"]
  key_links:
    - from: "internal/services/instance/resource_instance_acc_test.go"
      to: "internal/acctest/helpers.go"
      via: "ProtoV6ProviderFactories in resource.Test"
      pattern: "acctest\\.ProtoV6ProviderFactories"
    - from: "internal/services/template/resource_template_acc_test.go"
      to: "internal/acctest/helpers.go"
      via: "ProtoV6ProviderFactories in resource.Test"
      pattern: "acctest\\.ProtoV6ProviderFactories"
    - from: "internal/services/sshkey/resource_ssh_key_acc_test.go"
      to: "internal/acctest/helpers.go"
      via: "ProtoV6ProviderFactories in resource.Test"
      pattern: "acctest\\.ProtoV6ProviderFactories"
---

<objective>
Implement TF_ACC-gated acceptance tests for all Phase 2 resources and data sources, covering create, read, update, import, and destroy lifecycle for each.

Purpose: Acceptance tests (TEST-01, D-23) validate that the provider works against the real Vast.ai API. Unit tests verify schema and logic correctness, but only acceptance tests prove that API integration, state management, and the full Terraform lifecycle work end-to-end. This is a locked decision (D-23) requiring full coverage.

Output: Acceptance test files for all 3 resources and 5 data sources, all gated behind TF_ACC=1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-compute/02-CONTEXT.md
@.planning/phases/02-core-compute/02-RESEARCH.md
@.planning/phases/02-core-compute/02-01-SUMMARY.md
@.planning/phases/02-core-compute/02-05-SUMMARY.md

<interfaces>
<!-- Acceptance test helper from Plan 01 -->

From internal/acctest/helpers.go:
```go
package acctest

var ProtoV6ProviderFactories = map[string]func() (tfprotov6.ProviderServer, error){
    "vastai": providerserver.NewProtocol6WithError(provider.New("test")()),
}
```

<!-- All resource/data-source type names registered in provider -->
Resources: vastai_instance, vastai_template, vastai_ssh_key
Data sources: vastai_gpu_offers, vastai_instance, vastai_instances, vastai_templates, vastai_ssh_keys

<!-- Acceptance test framework pattern -->
```go
resource.Test(t, resource.TestCase{
    ProtoV6ProviderFactories: acctest.ProtoV6ProviderFactories,
    Steps: []resource.TestStep{
        {
            Config: testConfig,
            Check: resource.ComposeAggregateTestCheckFunc(
                resource.TestCheckResourceAttr("vastai_instance.test", "status", "running"),
                // ... more checks
            ),
        },
    },
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acceptance tests for SSH key and template resources (low cost, fast)</name>
  <files>
    internal/services/sshkey/resource_ssh_key_acc_test.go,
    internal/services/template/resource_template_acc_test.go,
    internal/services/offer/data_source_gpu_offers_acc_test.go
  </files>
  <read_first>
    internal/services/sshkey/resource_ssh_key.go,
    internal/services/sshkey/data_source_ssh_keys.go,
    internal/services/sshkey/models.go,
    internal/services/template/resource_template.go,
    internal/services/template/data_source_templates.go,
    internal/services/template/models.go,
    internal/services/offer/data_source_gpu_offers.go,
    internal/services/offer/models.go,
    internal/acctest/helpers.go,
    internal/client/ssh_keys.go,
    internal/client/templates.go,
    internal/client/offers.go,
    .planning/phases/02-core-compute/02-RESEARCH.md
  </read_first>
  <action>
    **Create internal/services/sshkey/resource_ssh_key_acc_test.go:**
    All functions prefixed with `TestAcc` and gated by `TF_ACC`. Use `acctest.ProtoV6ProviderFactories`.

    - `TestAccSSHKey_basic`: Create, read, destroy lifecycle.
      - Config: `resource "vastai_ssh_key" "test" { ssh_key = "ssh-ed25519 AAAA...test-key acceptance-test" }`
      - Step 1 (create): Check `resource.TestCheckResourceAttrSet("vastai_ssh_key.test", "id")`, Check `resource.TestCheckResourceAttrSet("vastai_ssh_key.test", "created_at")`
      - Destroy verified automatically by framework's CheckDestroy

    - `TestAccSSHKey_update`: Create then update with new key value.
      - Step 1: Create with initial key
      - Step 2: Update config with different ssh_key value, verify id persists (in-place update), ssh_key changed

    - `TestAccSSHKey_import`: Create then import.
      - Step 1: Create resource
      - Step 2: `ImportState` step with `ResourceName: "vastai_ssh_key.test"`, `ImportStateVerify: true`, `ImportStateVerifyIgnore: []string{"timeouts"}` (timeouts not stored in API)

    - `TestAccSSHKeysDataSource_basic`: Create an SSH key, then read via data source.
      - Step 1: Config creates ssh_key resource AND references `data "vastai_ssh_keys" "all" {}`
      - Check that `data.vastai_ssh_keys.all.ssh_keys.#` is at least 1

    **Create internal/services/template/resource_template_acc_test.go:**
    All functions prefixed with `TestAcc`.

    - `TestAccTemplate_basic`: Create, read, destroy.
      - Config: `resource "vastai_template" "test" { name = "tf-acc-test-RANDOM", image = "ubuntu:22.04" }`
      - Use a random suffix to avoid name collisions: `fmt.Sprintf("tf-acc-test-%d", rng.Int())`
      - Check: `resource.TestCheckResourceAttrSet("vastai_template.test", "id")` (hash_id)
      - Check: `resource.TestCheckResourceAttr("vastai_template.test", "name", expectedName)`
      - Check: `resource.TestCheckResourceAttr("vastai_template.test", "image", "ubuntu:22.04")`

    - `TestAccTemplate_update`: Create then update name/image.
      - Step 1: Create with initial name and image
      - Step 2: Update config with new name, verify in-place update (same id)

    - `TestAccTemplate_import`: Create then import by hash_id.
      - Step 1: Create resource
      - Step 2: `ImportState` with `ImportStateVerify: true`, `ImportStateVerifyIgnore: []string{"timeouts"}`

    - `TestAccTemplatesDataSource_basic`: Create a template, then search via data source.
      - Config creates template AND references `data "vastai_templates" "search" { query = "tf-acc-test" }`
      - Depends on the created template existing; check `data.vastai_templates.search.templates.#` >= 1

    **Create internal/services/offer/data_source_gpu_offers_acc_test.go:**

    - `TestAccGpuOffersDataSource_basic`: Search with no filters (just limit).
      - Config: `data "vastai_gpu_offers" "test" { limit = 3 }`
      - Check: `resource.TestCheckResourceAttr("data.vastai_gpu_offers.test", "offers.#", "3")` or `resource.TestCheckResourceAttrSet` on `offers.0.id`
      - Check: `resource.TestCheckResourceAttrSet("data.vastai_gpu_offers.test", "most_affordable.id")`

    - `TestAccGpuOffersDataSource_filtered`: Search with specific filters.
      - Config: `data "vastai_gpu_offers" "filtered" { num_gpus = 1, max_price_per_hour = 5.0, limit = 5 }`
      - Check: offers returned, each has expected attributes set

    Generate SSH key for tests using `ssh-keygen -t ed25519 -f /dev/null -N "" -C "acc-test"` or hardcode a valid test key.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go build ./internal/services/sshkey/... ./internal/services/template/... ./internal/services/offer/... && go test -v -count=1 -timeout 60s -run "^$" ./internal/services/sshkey/... ./internal/services/template/... ./internal/services/offer/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/sshkey/resource_ssh_key_acc_test.go contains `func TestAccSSHKey_basic(`
    - internal/services/sshkey/resource_ssh_key_acc_test.go contains `func TestAccSSHKey_update(`
    - internal/services/sshkey/resource_ssh_key_acc_test.go contains `func TestAccSSHKey_import(`
    - internal/services/sshkey/resource_ssh_key_acc_test.go contains `func TestAccSSHKeysDataSource_basic(`
    - internal/services/sshkey/resource_ssh_key_acc_test.go contains `acctest.ProtoV6ProviderFactories`
    - internal/services/template/resource_template_acc_test.go contains `func TestAccTemplate_basic(`
    - internal/services/template/resource_template_acc_test.go contains `func TestAccTemplate_update(`
    - internal/services/template/resource_template_acc_test.go contains `func TestAccTemplate_import(`
    - internal/services/template/resource_template_acc_test.go contains `func TestAccTemplatesDataSource_basic(`
    - internal/services/template/resource_template_acc_test.go contains `ImportState`
    - internal/services/offer/data_source_gpu_offers_acc_test.go contains `func TestAccGpuOffersDataSource_basic(`
    - internal/services/offer/data_source_gpu_offers_acc_test.go contains `func TestAccGpuOffersDataSource_filtered(`
    - internal/services/offer/data_source_gpu_offers_acc_test.go contains `most_affordable`
    - All test files compile: `go build ./internal/services/sshkey/... ./internal/services/template/... ./internal/services/offer/...` exits 0
    - Unit tests still pass (run with empty pattern to verify compilation without executing acceptance tests): `go test -run "^$" ./internal/services/...` exits 0
  </acceptance_criteria>
  <done>Acceptance tests exist for SSH key (create/update/import/destroy + data source), template (create/update/import/destroy + data source), and GPU offers data source (basic + filtered). All gated behind TF_ACC. Files compile.</done>
</task>

<task type="auto">
  <name>Task 2: Acceptance tests for instance resource and data sources (higher cost, full lifecycle)</name>
  <files>
    internal/services/instance/resource_instance_acc_test.go,
    internal/services/instance/data_source_instance_acc_test.go
  </files>
  <read_first>
    internal/services/instance/resource_instance.go,
    internal/services/instance/data_source_instance.go,
    internal/services/instance/data_source_instances.go,
    internal/services/instance/models.go,
    internal/services/offer/data_source_gpu_offers.go,
    internal/services/sshkey/resource_ssh_key.go,
    internal/acctest/helpers.go,
    internal/client/instances.go,
    .planning/phases/02-core-compute/02-RESEARCH.md
  </read_first>
  <action>
    **Create internal/services/instance/resource_instance_acc_test.go:**
    All functions prefixed with `TestAcc` and gated by `TF_ACC`. Use `acctest.ProtoV6ProviderFactories`.

    **Cost strategy per D-21:** Use the cheapest available offer. Include a helper that uses `data.vastai_gpu_offers` with low price limit:
    ```go
    const testInstanceBaseConfig = `
    data "vastai_gpu_offers" "cheapest" {
      num_gpus          = 1
      max_price_per_hour = 0.50
      order_by          = "dph_total"
      limit             = 1
    }
    `
    ```

    - `TestAccInstance_basic`: Create instance from cheapest offer, verify running, destroy.
      - Config: testInstanceBaseConfig + `resource "vastai_instance" "test" { offer_id = data.vastai_gpu_offers.cheapest.offers[0].id, image = "ubuntu:22.04", disk_gb = 10 }`
      - Step 1 (create+read): Check `resource.TestCheckResourceAttrSet("vastai_instance.test", "id")`, Check `resource.TestCheckResourceAttr("vastai_instance.test", "actual_status", "running")` or TestCheckResourceAttrSet for dynamic fields, Check `resource.TestCheckResourceAttrSet("vastai_instance.test", "ssh_host")`, Check `resource.TestCheckResourceAttrSet("vastai_instance.test", "gpu_name")`
      - CheckDestroy: Verify instance no longer exists (GET returns 404)

    - `TestAccInstance_update`: Create, then update label and verify in-place change.
      - Step 1: Create with `label = "acc-test-initial"`
      - Step 2: Update config to `label = "acc-test-updated"`, verify `resource.TestCheckResourceAttr("vastai_instance.test", "label", "acc-test-updated")`, verify id unchanged (in-place)

    - `TestAccInstance_import`: Create then import by contract ID.
      - Step 1: Create resource
      - Step 2: `ImportState` with `ImportStateVerify: true`, `ImportStateVerifyIgnore: []string{"offer_id", "disk_gb", "timeouts", "ssh_key_ids", "image_login", "cancel_unavail", "env", "use_ssh", "use_jupyter_lab"}` (attributes not returned from Read or only set at creation)

    **Handle flakiness:** Instance creation depends on offer availability. If the cheapest offer fails, the test should produce a clear error. Use `resource.TestStep.ExpectNonEmptyPlan: true` on updates where computed fields may change between API calls. Set per D-22 `t.Parallel()` only if Vast.ai API supports concurrent operations; otherwise run sequentially.

    **CheckDestroy helper:**
    ```go
    func testAccCheckInstanceDestroy(s *terraform.State) error {
        // For each vastai_instance in state, verify GET returns 404
    }
    ```

    **Create internal/services/instance/data_source_instance_acc_test.go:**

    - `TestAccInstanceDataSource_basic`: Create an instance, then look it up by ID.
      - Config creates instance resource AND references `data "vastai_instance" "lookup" { id = vastai_instance.test.id }`
      - Check data source has matching gpu_name, actual_status

    - `TestAccInstancesDataSource_basic`: Create an instance with a label, then list with label filter.
      - Config creates instance with `label = "acc-test-ds-RANDOM"` AND references `data "vastai_instances" "filtered" { label = "acc-test-ds-RANDOM" }`
      - Check `data.vastai_instances.filtered.instances.#` >= 1

    **Timeout for instance tests:** Instance creation can take several minutes. Set `resource.Test` timeout appropriately or use `resource.TestStep` with sufficient Terraform timeouts in the config:
    ```hcl
    resource "vastai_instance" "test" {
      # ...
      timeouts {
        create = "15m"
        delete = "5m"
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go build ./internal/services/instance/... && go test -v -count=1 -timeout 60s -run "^$" ./internal/services/instance/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/services/instance/resource_instance_acc_test.go contains `func TestAccInstance_basic(`
    - internal/services/instance/resource_instance_acc_test.go contains `func TestAccInstance_update(`
    - internal/services/instance/resource_instance_acc_test.go contains `func TestAccInstance_import(`
    - internal/services/instance/resource_instance_acc_test.go contains `acctest.ProtoV6ProviderFactories`
    - internal/services/instance/resource_instance_acc_test.go contains `data.vastai_gpu_offers` (uses cheapest offer)
    - internal/services/instance/resource_instance_acc_test.go contains `ImportState`
    - internal/services/instance/resource_instance_acc_test.go contains `testAccCheckInstanceDestroy` or similar CheckDestroy function
    - internal/services/instance/data_source_instance_acc_test.go contains `func TestAccInstanceDataSource_basic(`
    - internal/services/instance/data_source_instance_acc_test.go contains `func TestAccInstancesDataSource_basic(`
    - All test files compile: `go build ./internal/services/instance/...` exits 0
    - Unit tests still pass: `go test -run "^$" ./internal/services/instance/...` exits 0
  </acceptance_criteria>
  <done>Acceptance tests exist for instance resource (create/update/import/destroy), instance data source (read by ID), instances data source (list with filter). All use cheapest available offer per D-21. All gated behind TF_ACC. CheckDestroy verifies cleanup. Files compile.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles all acceptance test files
- `go test -run "^$" -count=1 ./internal/...` verifies all test files compile (runs no tests)
- `go test -v -count=1 -parallel=4 -timeout 120s ./internal/...` unit tests still pass (acceptance tests skipped without TF_ACC)
- When TF_ACC=1: `TF_ACC=1 go test -v -count=1 -timeout 120m ./internal/services/sshkey/ -run TestAcc` runs SSH key acceptance tests
- When TF_ACC=1: `TF_ACC=1 go test -v -count=1 -timeout 120m ./internal/services/template/ -run TestAcc` runs template acceptance tests
- When TF_ACC=1: `TF_ACC=1 go test -v -count=1 -timeout 120m ./internal/services/instance/ -run TestAcc` runs instance acceptance tests
- When TF_ACC=1: `TF_ACC=1 go test -v -count=1 -timeout 120m ./internal/services/offer/ -run TestAcc` runs offer data source acceptance tests
</verification>

<success_criteria>
- Every resource (vastai_instance, vastai_template, vastai_ssh_key) has acceptance tests covering create, read, update, import, and destroy (TEST-01, D-23)
- Every data source (vastai_gpu_offers, vastai_instance, vastai_instances, vastai_templates, vastai_ssh_keys) has at least one acceptance test covering read (TEST-01, D-23)
- All acceptance tests are gated behind TF_ACC=1 (not run during normal `go test`)
- Instance tests use cheapest available offer to minimize cost (D-21)
- All test files compile without TF_ACC set
- Existing unit tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-compute/02-06-SUMMARY.md`
</output>
