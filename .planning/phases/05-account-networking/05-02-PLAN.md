---
phase: 05-account-networking
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - internal/client/clusters.go
  - internal/client/clusters_test.go
  - internal/client/overlays.go
  - internal/client/overlays_test.go
  - internal/client/users.go
  - internal/client/users_test.go
  - internal/client/invoices.go
  - internal/client/invoices_test.go
  - internal/client/audit_logs.go
  - internal/client/audit_logs_test.go
  - internal/client/client.go
autonomous: true
requirements: [NETW-01, NETW-02, NETW-03, NETW-04, DATA-07, DATA-10, DATA-11]

must_haves:
  truths:
    - "All 5 networking/data client services compile and pass unit tests"
    - "InvoiceService uses GetFullPath for v1 API endpoint"
    - "VastAIClient has all 9 service sub-objects initialized (4 from 05-01 + 5 new)"
  artifacts:
    - path: "internal/client/clusters.go"
      provides: "ClusterService with Create, List, Delete, JoinMachine, RemoveMachine"
      exports: ["ClusterService", "Cluster", "ClusterNode"]
    - path: "internal/client/overlays.go"
      provides: "OverlayService with Create, List, Delete, JoinInstance"
      exports: ["OverlayService", "Overlay"]
    - path: "internal/client/users.go"
      provides: "UserService with GetCurrent"
      exports: ["UserService", "User"]
    - path: "internal/client/invoices.go"
      provides: "InvoiceService with List (using GetFullPath for v1 API)"
      exports: ["InvoiceService", "Invoice", "InvoiceListResponse"]
    - path: "internal/client/audit_logs.go"
      provides: "AuditLogService with List"
      exports: ["AuditLogService", "AuditLogEntry"]
    - path: "internal/client/client.go"
      provides: "VastAIClient with remaining 5 service sub-objects added"
  key_links:
    - from: "internal/client/invoices.go"
      to: "internal/client/auth.go"
      via: "GetFullPath -> newRequestFullPath"
      pattern: "GetFullPath.*newRequestFullPath"
    - from: "internal/client/client.go"
      to: "all service files"
      via: "service sub-object initialization in NewVastAIClient"
      pattern: "Clusters.*=.*&ClusterService"
---

<objective>
Create Go API client services for networking (clusters, overlays) and read-only data (users, invoices, audit logs). Also add remaining 5 service sub-objects to VastAIClient.

Purpose: These client services back the networking Terraform resources and read-only data sources. Cluster and overlay services implement the create-then-read pattern. Invoice service uses the v1 API endpoint via GetFullPath (created in Plan 05-01).

Output: 5 new client service files with httptest unit tests, updated client.go with all 9 service sub-objects complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-account-networking/05-RESEARCH.md
@.planning/phases/05-account-networking/05-01-SUMMARY.md

@internal/client/client.go
@internal/client/auth.go
@internal/client/ssh_keys.go
@internal/client/ssh_keys_test.go

<interfaces>
<!-- Methods from Plan 05-01 that this plan uses -->

From internal/client/client.go (created in 05-01):
```go
func (c *VastAIClient) Get(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) Post(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Put(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Delete(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) DeleteWithBody(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) GetFullPath(ctx context.Context, fullPath string, result interface{}) error
```

Service sub-object pattern (from client.go):
```go
type VastAIClient struct {
    // ...existing + 4 account sub-objects from 05-01...
    ApiKeys      *ApiKeyService
    EnvVars      *EnvVarService
    Teams        *TeamService
    Subaccounts  *SubaccountService
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Networking client services (clusters, overlays)</name>
  <files>
    internal/client/clusters.go, internal/client/clusters_test.go,
    internal/client/overlays.go, internal/client/overlays_test.go
  </files>
  <read_first>
    internal/client/ssh_keys.go,
    internal/client/ssh_keys_test.go,
    internal/client/endpoints.go,
    internal/client/endpoints_test.go,
    internal/client/client.go
  </read_first>
  <action>
    Create 2 networking client service files following the exact pattern from ssh_keys.go and endpoints.go.

    **clusters.go** (NETW-01, NETW-03):
    - `ClusterService` struct with `client *VastAIClient`
    - `ClusterNode` struct: `MachineID int` (json:"machine_id"), `IsClusterManager bool` (json:"is_cluster_manager"), `LocalIP string` (json:"local_ip")
    - `Cluster` struct: `ID int`, `Subnet string`, `Nodes []ClusterNode`
    - `ClusterListResponse` struct: `Clusters map[string]Cluster` (json:"clusters") -- the API uses string IDs as map keys
    - `Create(ctx, subnet string, managerID int) (*Cluster, error)` -- POST `/cluster/` with `{"subnet": subnet, "manager_id": managerID}`. Returns only msg. Use create-then-read: after create, call List and find cluster matching subnet (Pitfall 4).
    - `List(ctx) (map[string]Cluster, error)` -- GET `/clusters/`. Parse response as ClusterListResponse, return .Clusters map. Populate Cluster.ID from map key (parse string to int).
    - `Delete(ctx, clusterID int) error` -- DELETE `/cluster/` using DeleteWithBody with `{"cluster_id": clusterID}` (Pitfall 7).
    - `JoinMachine(ctx, clusterID int, machineIDs []int) error` -- PUT `/cluster/` with `{"cluster_id": clusterID, "machine_ids": machineIDs}`
    - `RemoveMachine(ctx, clusterID, machineID int, newManagerID *int) error` -- DELETE `/cluster/remove_machine/` using DeleteWithBody with `{"cluster_id": clusterID, "machine_id": machineID}`. Include `new_manager_id` only if non-nil.

    **overlays.go** (NETW-02, NETW-04):
    - `OverlayService` struct with `client *VastAIClient`
    - `Overlay` struct: `OverlayID int` (json:"overlay_id"), `Name string`, `InternalSubnet string` (json:"internal_subnet"), `ClusterID int` (json:"cluster_id"), `Instances []int` (json:"instances")
    - `Create(ctx, clusterID int, name string) (*Overlay, error)` -- POST `/overlay/` with `{"cluster_id": clusterID, "name": name}`. Returns only msg. Use create-then-read: list and find by name (Pitfall 4).
    - `List(ctx) ([]Overlay, error)` -- GET `/overlay/`
    - `Delete(ctx, overlayID int) error` -- DELETE `/overlay/` using DeleteWithBody with `{"overlay_id": overlayID}`
    - `JoinInstance(ctx, name string, instanceID int) error` -- PUT `/overlay/` with `{"name": name, "instance_id": instanceID}`

    For both test files: httptest mock server, test success and error paths. For create-then-read operations (clusters, overlays), mock both the create endpoint and the list endpoint in the same test.
  </action>
  <acceptance_criteria>
    - grep -r "type ClusterService struct" internal/client/clusters.go returns a match
    - grep -r "type OverlayService struct" internal/client/overlays.go returns a match
    - grep -r "DeleteWithBody" internal/client/clusters.go returns a match (Pitfall 7)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -timeout 60s -run "TestCluster|TestOverlay" 2>&1 | tail -20</automated>
  </verify>
  <done>Cluster and overlay client services compile with passing tests. Create-then-read pattern works for both. DeleteWithBody used for cluster deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Data client services (users, invoices, audit logs) + client.go wiring</name>
  <files>
    internal/client/users.go, internal/client/users_test.go,
    internal/client/invoices.go, internal/client/invoices_test.go,
    internal/client/audit_logs.go, internal/client/audit_logs_test.go,
    internal/client/client.go
  </files>
  <read_first>
    internal/client/ssh_keys.go,
    internal/client/ssh_keys_test.go,
    internal/client/client.go
  </read_first>
  <action>
    Create 3 read-only data client service files and add remaining 5 service sub-objects to VastAIClient.

    **users.go** (DATA-07):
    - `UserService` struct with `client *VastAIClient`
    - `User` struct with fields from SDK's user_fields tuple (per D-05 discretion, include focused subset):
      `ID int`, `Username string`, `Email string`, `EmailVerified bool` (json:"email_verified"),
      `Fullname string`, `Balance float64`, `Credit float64`,
      `HasBilling bool` (json:"has_billing"), `SSHKey string` (json:"ssh_key"),
      `BalanceThreshold float64` (json:"balance_threshold"),
      `BalanceThresholdEnabled bool` (json:"balance_threshold_enabled")
    - `GetCurrent(ctx) (*User, error)` -- GET `/users/current?owner=me`

    **invoices.go** (DATA-10):
    - `InvoiceService` struct with `client *VastAIClient`
    - `Invoice` struct with fields matching v1 response (exact fields depend on API, but include common billing fields: `ID`, `Amount float64`, `Type string`, `Description string`, `Timestamp string`, etc.)
    - `InvoiceListResponse` struct: `Results []Invoice` (json:"results"), `Count int` (json:"count"), `Total int` (json:"total"), `NextToken string` (json:"next_token")
    - `InvoiceListParams` struct: `StartDate string`, `EndDate string`, `Limit int`, `Type string` (per D-06: support date filtering and type)
    - `List(ctx, params InvoiceListParams) (*InvoiceListResponse, error)` -- Uses GetFullPath with `/api/v1/invoices/`. Build query string with url.Values from params (only include non-empty/non-zero values). Call `s.client.GetFullPath(ctx, "/api/v1/invoices/?"+queryString, &resp)`.

    **audit_logs.go** (DATA-11):
    - `AuditLogService` struct with `client *VastAIClient`
    - `AuditLogEntry` struct: `IPAddress string` (json:"ip_address"), `ApiKeyID int` (json:"api_key_id"), `CreatedAt string` (json:"created_at"), `ApiRoute string` (json:"api_route"), `Args string` (json:"args")
    - `List(ctx) ([]AuditLogEntry, error)` -- GET `/audit_logs/`. Per D-07: no filtering needed, return all.

    **client.go updates:**
    Add 5 remaining service sub-object fields to VastAIClient struct:
    `Clusters *ClusterService`, `Overlays *OverlayService`,
    `Users *UserService`, `Invoices *InvoiceService`, `AuditLogs *AuditLogService`
    Initialize all 5 in NewVastAIClient following existing pattern.

    For all test files: httptest mock server, test success and error paths. For invoices test: verify the test server receives a request to `/api/v1/invoices/` (NOT `/api/v0/api/v1/invoices/`) confirming GetFullPath works correctly.
  </action>
  <acceptance_criteria>
    - grep -r "type UserService struct" internal/client/users.go returns a match
    - grep -r "type InvoiceService struct" internal/client/invoices.go returns a match
    - grep -r "type AuditLogService struct" internal/client/audit_logs.go returns a match
    - grep -r "GetFullPath" internal/client/invoices.go returns a match (invoices uses v1 API)
    - grep -r "/api/v1/invoices/" internal/client/invoices.go returns a match
    - grep -r "/audit_logs/" internal/client/audit_logs.go returns a match
    - grep -r "/users/current" internal/client/users.go returns a match
    - grep -r "Clusters.*\*ClusterService" internal/client/client.go returns a match
    - grep -r "Invoices.*\*InvoiceService" internal/client/client.go returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -timeout 60s 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 read-only data source services compile with passing tests. InvoiceService correctly uses GetFullPath for v1 API endpoint. VastAIClient has all 9 service sub-objects initialized.</done>
</task>

</tasks>

<verification>
All client services compile and pass unit tests:
```bash
cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -v -timeout 120s
```

Full project compiles:
```bash
cd /home/aescalera/code/ai/infrastructure/vastai && go build ./...
```
</verification>

<success_criteria>
- 5 new client service files exist in internal/client/ with corresponding test files
- VastAIClient struct has all 9 new service sub-objects complete
- InvoiceService uses GetFullPath for v1 API path
- Create-then-read pattern works for clusters and overlays
- All unit tests pass: `go test ./internal/client/... -count=1`
- Full project compiles: `go build ./...`
</success_criteria>

<output>
After completion, create `.planning/phases/05-account-networking/05-02-SUMMARY.md`
</output>
