---
phase: 05-account-networking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/api_keys.go
  - internal/client/api_keys_test.go
  - internal/client/env_vars.go
  - internal/client/env_vars_test.go
  - internal/client/teams.go
  - internal/client/teams_test.go
  - internal/client/subaccounts.go
  - internal/client/subaccounts_test.go
  - internal/client/clusters.go
  - internal/client/clusters_test.go
  - internal/client/overlays.go
  - internal/client/overlays_test.go
  - internal/client/users.go
  - internal/client/users_test.go
  - internal/client/invoices.go
  - internal/client/invoices_test.go
  - internal/client/audit_logs.go
  - internal/client/audit_logs_test.go
  - internal/client/client.go
  - internal/client/auth.go
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04, ACCT-05, ACCT-06, NETW-01, NETW-02, NETW-03, NETW-04, DATA-07, DATA-10, DATA-11]

must_haves:
  truths:
    - "All 9 client services compile and pass unit tests"
    - "GetFullPath method supports /api/v1/ prefix for invoices"
    - "VastAIClient has all new service sub-objects initialized"
  artifacts:
    - path: "internal/client/api_keys.go"
      provides: "ApiKeyService with Create, List, Delete"
      exports: ["ApiKeyService", "ApiKey"]
    - path: "internal/client/env_vars.go"
      provides: "EnvVarService with Create, List, Update, Delete"
      exports: ["EnvVarService", "EnvVarMap"]
    - path: "internal/client/teams.go"
      provides: "TeamService with team CRUD, role CRUD, member invite/list/remove"
      exports: ["TeamService", "Team", "TeamRole", "TeamMember"]
    - path: "internal/client/subaccounts.go"
      provides: "SubaccountService with Create, List"
      exports: ["SubaccountService", "Subaccount"]
    - path: "internal/client/clusters.go"
      provides: "ClusterService with Create, List, Delete, JoinMachine, RemoveMachine"
      exports: ["ClusterService", "Cluster", "ClusterNode"]
    - path: "internal/client/overlays.go"
      provides: "OverlayService with Create, List, Delete, JoinInstance"
      exports: ["OverlayService", "Overlay"]
    - path: "internal/client/users.go"
      provides: "UserService with GetCurrent"
      exports: ["UserService", "User"]
    - path: "internal/client/invoices.go"
      provides: "InvoiceService with List (using GetFullPath for v1 API)"
      exports: ["InvoiceService", "Invoice", "InvoiceListResponse"]
    - path: "internal/client/audit_logs.go"
      provides: "AuditLogService with List"
      exports: ["AuditLogService", "AuditLogEntry"]
    - path: "internal/client/client.go"
      provides: "VastAIClient with 9 new service sub-objects"
    - path: "internal/client/auth.go"
      provides: "newRequestFullPath method for non-v0 API endpoints"
  key_links:
    - from: "internal/client/invoices.go"
      to: "internal/client/auth.go"
      via: "GetFullPath -> newRequestFullPath"
      pattern: "GetFullPath.*newRequestFullPath"
    - from: "internal/client/client.go"
      to: "all new service files"
      via: "service sub-object initialization in NewVastAIClient"
      pattern: "ApiKeys.*=.*&ApiKeyService"
---

<objective>
Create all Go API client services for Phase 5: account management (API keys, environment variables, teams with roles/members, subaccounts), networking (clusters, overlays), and read-only data (users, invoices, audit logs). Also add the GetFullPath client method needed for the invoices v1 API endpoint.

Purpose: Every Terraform resource and data source in Phase 5 depends on these client services. Building them first with full unit test coverage ensures the Terraform layer can be developed against stable, tested API contracts.

Output: 9 new client service files with httptest unit tests, updated client.go with service sub-objects, updated auth.go with newRequestFullPath.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-account-networking/05-RESEARCH.md

@internal/client/client.go
@internal/client/auth.go
@internal/client/ssh_keys.go
@internal/client/ssh_keys_test.go
@internal/client/endpoints.go
@internal/client/endpoints_test.go

<interfaces>
<!-- Existing client methods the new services will use -->

From internal/client/client.go:
```go
func (c *VastAIClient) Get(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) Post(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Put(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Delete(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) DeleteWithBody(ctx context.Context, path string, body, result interface{}) error
```

From internal/client/auth.go:
```go
func (c *VastAIClient) newRequest(ctx context.Context, method, path string, body interface{}) (*retryablehttp.Request, error)
// URL construction: c.baseURL + "/api/v0" + path
```

Service sub-object pattern (from client.go):
```go
type VastAIClient struct {
    // ...
    Instances      *InstanceService
    Offers         *OfferService
    // ... etc
}
// Initialized in NewVastAIClient: c.Instances = &InstanceService{client: c}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client services for account and networking APIs</name>
  <files>
    internal/client/api_keys.go, internal/client/api_keys_test.go,
    internal/client/env_vars.go, internal/client/env_vars_test.go,
    internal/client/teams.go, internal/client/teams_test.go,
    internal/client/subaccounts.go, internal/client/subaccounts_test.go,
    internal/client/clusters.go, internal/client/clusters_test.go,
    internal/client/overlays.go, internal/client/overlays_test.go,
    internal/client/auth.go, internal/client/client.go
  </files>
  <read_first>
    internal/client/ssh_keys.go,
    internal/client/ssh_keys_test.go,
    internal/client/endpoints.go,
    internal/client/endpoints_test.go,
    internal/client/client.go,
    internal/client/auth.go
  </read_first>
  <action>
    Create 6 client service files following the exact pattern from ssh_keys.go and endpoints.go.
    Each service file has: typed API model structs, a Service struct with `client *VastAIClient`, and methods for each API operation.
    Each test file has: httptest mock server, tests for each operation covering success and error paths.

    **api_keys.go** (ACCT-01):
    - `ApiKeyService` struct with `client *VastAIClient`
    - `ApiKey` struct: `ID int`, `Name string`, `Key string` (json omitempty -- only on create), `Permissions json.RawMessage`, `KeyParams string` (omitempty), `CreatedAt string` (omitempty)
    - `Create(ctx, name string, permissions json.RawMessage) (*ApiKey, error)` -- POST `/auth/apikeys/` with `{"name": name, "permissions": perms}`. Returns full ApiKey including key value.
    - `List(ctx) ([]ApiKey, error)` -- GET `/auth/apikeys/`. Returns array of ApiKey (without key value).
    - `Delete(ctx, id int) error` -- DELETE `/auth/apikeys/{id}/`. Standard path-based delete.
    - No Update method -- API keys are immutable after creation per research.

    **env_vars.go** (ACCT-02):
    - `EnvVarService` struct with `client *VastAIClient`
    - `EnvVarMap` struct: `Secrets map[string]string` (json:"secrets")
    - `Create(ctx, key, value string) error` -- POST `/secrets/` with `{"key": key, "value": value}`
    - `List(ctx) (map[string]string, error)` -- GET `/secrets/` returns EnvVarMap, extract .Secrets
    - `Update(ctx, key, value string) error` -- PUT `/secrets/` with `{"key": key, "value": value}`
    - `Delete(ctx, key string) error` -- DELETE `/secrets/` using DeleteWithBody with `{"key": key}` (Pitfall 2)

    **teams.go** (ACCT-03, ACCT-04, ACCT-05):
    Combined service for team, roles, and members since they share the `/team/` API namespace.
    - `TeamService` struct with `client *VastAIClient`
    - `Team` struct: `ID int`, `TeamName string` (json:"team_name"), plus any other returned fields
    - `TeamRole` struct: `ID int`, `Name string`, `Permissions json.RawMessage`
    - `TeamMember` struct: `ID int`, `Email string`, `Role string`, plus other returned fields
    - Team CRUD:
      - `CreateTeam(ctx, teamName string) (*Team, error)` -- POST `/team/` with `{"team_name": name}`
      - `DestroyTeam(ctx) error` -- DELETE `/team/` (no params, parameterless delete per research)
    - Role CRUD:
      - `CreateRole(ctx, name string, permissions json.RawMessage) (*TeamRole, error)` -- POST `/team/roles/` with `{"name": name, "permissions": perms}`
      - `ListRoles(ctx) ([]TeamRole, error)` -- GET `/team/roles-full/`
      - `GetRole(ctx, name string) (*TeamRole, error)` -- GET `/team/roles/{name}/`
      - `UpdateRole(ctx, id int, name string, permissions json.RawMessage) (*TeamRole, error)` -- PUT `/team/roles/{id}/` (Pitfall 3: update uses ID, not name)
      - `DeleteRole(ctx, name string) error` -- DELETE `/team/roles/{name}/` (Pitfall 3: delete uses name)
    - Member operations:
      - `InviteMember(ctx, email, role string) error` -- POST `/team/invite/?email={email}&role={role}` (Pitfall 5: query params, NOT JSON body). Encode email/role directly in URL path.
      - `ListMembers(ctx) ([]TeamMember, error)` -- GET `/team/members/`
      - `RemoveMember(ctx, id int) error` -- DELETE `/team/members/{id}/`

    **subaccounts.go** (ACCT-06):
    - `SubaccountService` struct with `client *VastAIClient`
    - `Subaccount` struct: `ID int`, `Email string`, `Username string`
    - `SubaccountCreateRequest` struct: `Email string`, `Username string`, `Password string`, `HostOnly bool` (json:"host_only"), `ParentID string` (json:"parent_id")
    - `SubaccountListResponse` struct: `Users []Subaccount` (json:"users")
    - `Create(ctx, email, username, password string, hostOnly bool) (*Subaccount, error)` -- POST `/users/` with parent_id="me". Parse response for ID.
    - `List(ctx) ([]Subaccount, error)` -- GET `/subaccounts?owner=me`. Unwrap .Users from response.
    - No Delete or Update methods -- API has no delete endpoint per research (Pitfall 8).

    **clusters.go** (NETW-01, NETW-03):
    - `ClusterService` struct with `client *VastAIClient`
    - `ClusterNode` struct: `MachineID int` (json:"machine_id"), `IsClusterManager bool` (json:"is_cluster_manager"), `LocalIP string` (json:"local_ip")
    - `Cluster` struct: `ID int`, `Subnet string`, `Nodes []ClusterNode`
    - `ClusterListResponse` struct: `Clusters map[string]Cluster` (json:"clusters") -- the API uses string IDs as map keys
    - `Create(ctx, subnet string, managerID int) (*Cluster, error)` -- POST `/cluster/` with `{"subnet": subnet, "manager_id": managerID}`. Returns only msg. Use create-then-read: after create, call List and find cluster matching subnet (Pitfall 4).
    - `List(ctx) (map[string]Cluster, error)` -- GET `/clusters/`. Parse response as ClusterListResponse, return .Clusters map. Populate Cluster.ID from map key (parse string to int).
    - `Delete(ctx, clusterID int) error` -- DELETE `/cluster/` using DeleteWithBody with `{"cluster_id": clusterID}` (Pitfall 7).
    - `JoinMachine(ctx, clusterID int, machineIDs []int) error` -- PUT `/cluster/` with `{"cluster_id": clusterID, "machine_ids": machineIDs}`
    - `RemoveMachine(ctx, clusterID, machineID int, newManagerID *int) error` -- DELETE `/cluster/remove_machine/` using DeleteWithBody with `{"cluster_id": clusterID, "machine_id": machineID}`. Include `new_manager_id` only if non-nil.

    **overlays.go** (NETW-02, NETW-04):
    - `OverlayService` struct with `client *VastAIClient`
    - `Overlay` struct: `OverlayID int` (json:"overlay_id"), `Name string`, `InternalSubnet string` (json:"internal_subnet"), `ClusterID int` (json:"cluster_id"), `Instances []int` (json:"instances")
    - `Create(ctx, clusterID int, name string) (*Overlay, error)` -- POST `/overlay/` with `{"cluster_id": clusterID, "name": name}`. Returns only msg. Use create-then-read: list and find by name (Pitfall 4).
    - `List(ctx) ([]Overlay, error)` -- GET `/overlay/`
    - `Delete(ctx, overlayID int) error` -- DELETE `/overlay/` using DeleteWithBody with `{"overlay_id": overlayID}`
    - `JoinInstance(ctx, name string, instanceID int) error` -- PUT `/overlay/` with `{"name": name, "instance_id": instanceID}`

    **auth.go changes:**
    Add `newRequestFullPath` method that constructs URL as `c.baseURL + fullPath` (no `/api/v0` prefix). Same body encoding and header logic as existing newRequest. This is needed for invoices v1 endpoint (Pitfall 6).

    **client.go changes:**
    - Add `GetFullPath(ctx, fullPath string, result interface{}) error` method that calls newRequestFullPath + do.
    - Add 9 new service sub-object fields to VastAIClient struct:
      `ApiKeys *ApiKeyService`, `EnvVars *EnvVarService`, `Teams *TeamService`,
      `Subaccounts *SubaccountService`, `Clusters *ClusterService`, `Overlays *OverlayService`,
      `Users *UserService`, `Invoices *InvoiceService`, `AuditLogs *AuditLogService`
    - Initialize all 9 in NewVastAIClient following existing pattern.

    For all test files: use httptest.NewServer pattern from ssh_keys_test.go. Test success and error (non-200 status) for each operation. For create-then-read operations (clusters, overlays), mock both the create endpoint and the list endpoint in the same test.
  </action>
  <acceptance_criteria>
    - grep -r "type ApiKeyService struct" internal/client/api_keys.go returns a match
    - grep -r "type EnvVarService struct" internal/client/env_vars.go returns a match
    - grep -r "type TeamService struct" internal/client/teams.go returns a match
    - grep -r "type SubaccountService struct" internal/client/subaccounts.go returns a match
    - grep -r "type ClusterService struct" internal/client/clusters.go returns a match
    - grep -r "type OverlayService struct" internal/client/overlays.go returns a match
    - grep -r "DeleteWithBody" internal/client/env_vars.go returns a match (Pitfall 2)
    - grep -r "DeleteWithBody" internal/client/clusters.go returns a match (Pitfall 7)
    - grep -r "newRequestFullPath" internal/client/auth.go returns a match
    - grep -r "GetFullPath" internal/client/client.go returns a match
    - grep -r "ApiKeys.*\*ApiKeyService" internal/client/client.go returns a match
    - grep -r "team/invite/?" internal/client/teams.go returns a match (Pitfall 5: query params)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -timeout 60s 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 client service files compile, VastAIClient has 9 new service sub-objects, GetFullPath method exists, all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Client services for read-only data sources (user, invoices, audit logs)</name>
  <files>
    internal/client/users.go, internal/client/users_test.go,
    internal/client/invoices.go, internal/client/invoices_test.go,
    internal/client/audit_logs.go, internal/client/audit_logs_test.go
  </files>
  <read_first>
    internal/client/ssh_keys.go,
    internal/client/ssh_keys_test.go,
    internal/client/client.go
  </read_first>
  <action>
    Create 3 client service files for read-only data sources. These are simpler than CRUD services -- each has only a read/list operation.

    **users.go** (DATA-07):
    - `UserService` struct with `client *VastAIClient`
    - `User` struct with fields from SDK's user_fields tuple (per D-05 discretion, include focused subset):
      `ID int`, `Username string`, `Email string`, `EmailVerified bool` (json:"email_verified"),
      `Fullname string`, `Balance float64`, `Credit float64`,
      `HasBilling bool` (json:"has_billing"), `SSHKey string` (json:"ssh_key"),
      `BalanceThreshold float64` (json:"balance_threshold"),
      `BalanceThresholdEnabled bool` (json:"balance_threshold_enabled")
    - `GetCurrent(ctx) (*User, error)` -- GET `/users/current?owner=me`

    **invoices.go** (DATA-10):
    - `InvoiceService` struct with `client *VastAIClient`
    - `Invoice` struct with fields matching v1 response (exact fields depend on API, but include common billing fields: `ID`, `Amount float64`, `Type string`, `Description string`, `Timestamp string`, etc.)
    - `InvoiceListResponse` struct: `Results []Invoice` (json:"results"), `Count int` (json:"count"), `Total int` (json:"total"), `NextToken string` (json:"next_token")
    - `InvoiceListParams` struct: `StartDate string`, `EndDate string`, `Limit int`, `Type string` (per D-06: support date filtering and type)
    - `List(ctx, params InvoiceListParams) (*InvoiceListResponse, error)` -- Uses GetFullPath with `/api/v1/invoices/`. Build query string with url.Values from params (only include non-empty/non-zero values). Call `s.client.GetFullPath(ctx, "/api/v1/invoices/?"+queryString, &resp)`.

    **audit_logs.go** (DATA-11):
    - `AuditLogService` struct with `client *VastAIClient`
    - `AuditLogEntry` struct: `IPAddress string` (json:"ip_address"), `ApiKeyID int` (json:"api_key_id"), `CreatedAt string` (json:"created_at"), `ApiRoute string` (json:"api_route"), `Args string` (json:"args")
    - `List(ctx) ([]AuditLogEntry, error)` -- GET `/audit_logs/`. Per D-07: no filtering needed, return all.

    For all test files: httptest mock server, test success and error paths. For invoices test: verify the test server receives a request to `/api/v1/invoices/` (NOT `/api/v0/api/v1/invoices/`) confirming GetFullPath works correctly.
  </action>
  <acceptance_criteria>
    - grep -r "type UserService struct" internal/client/users.go returns a match
    - grep -r "type InvoiceService struct" internal/client/invoices.go returns a match
    - grep -r "type AuditLogService struct" internal/client/audit_logs.go returns a match
    - grep -r "GetFullPath" internal/client/invoices.go returns a match (invoices uses v1 API)
    - grep -r "/api/v1/invoices/" internal/client/invoices.go returns a match
    - grep -r "/audit_logs/" internal/client/audit_logs.go returns a match
    - grep -r "/users/current" internal/client/users.go returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -timeout 60s -run "TestUser|TestInvoice|TestAuditLog" 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 read-only data source services compile with passing tests. InvoiceService correctly uses GetFullPath for v1 API endpoint.</done>
</task>

</tasks>

<verification>
All client services compile and pass unit tests:
```bash
cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/... -count=1 -v -timeout 120s
```

Full project compiles:
```bash
cd /home/aescalera/code/ai/infrastructure/vastai && go build ./...
```
</verification>

<success_criteria>
- 9 new client service files exist in internal/client/ with corresponding test files
- VastAIClient struct has 9 new service sub-objects (ApiKeys, EnvVars, Teams, Subaccounts, Clusters, Overlays, Users, Invoices, AuditLogs)
- GetFullPath method on VastAIClient supports non-v0 API paths
- newRequestFullPath in auth.go constructs URLs without /api/v0 prefix
- All unit tests pass: `go test ./internal/client/... -count=1`
- Full project compiles: `go build ./...`
</success_criteria>

<output>
After completion, create `.planning/phases/05-account-networking/05-01-SUMMARY.md`
</output>
