---
phase: 04-serverless
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/endpoints.go
  - internal/client/endpoints_test.go
  - internal/client/worker_groups.go
  - internal/client/worker_groups_test.go
  - internal/client/client.go
autonomous: true
requirements: [SRVL-01, SRVL-02, SRVL-03, DATA-09]

must_haves:
  truths:
    - "EndpointService provides Create, List, Update, Delete methods with correct API paths"
    - "WorkerGroupService provides Create, List, Update, Delete methods with correct API paths"
    - "VastAIClient initializes Endpoints and WorkerGroups service sub-objects"
    - "All CRUD methods use the patterns extracted from the Python SDK (POST create, GET list, PUT update, DELETE-with-body)"
  artifacts:
    - path: "internal/client/endpoints.go"
      provides: "EndpointService with Create, List, Update, Delete and typed structs"
      exports: ["EndpointService", "CreateEndpointRequest", "UpdateEndpointRequest", "Endpoint", "endpointListResponse"]
    - path: "internal/client/worker_groups.go"
      provides: "WorkerGroupService with Create, List, Update, Delete and typed structs"
      exports: ["WorkerGroupService", "CreateWorkerGroupRequest", "UpdateWorkerGroupRequest", "WorkerGroup", "workerGroupListResponse"]
    - path: "internal/client/endpoints_test.go"
      provides: "Unit tests for EndpointService methods"
    - path: "internal/client/worker_groups_test.go"
      provides: "Unit tests for WorkerGroupService methods"
  key_links:
    - from: "internal/client/client.go"
      to: "internal/client/endpoints.go"
      via: "VastAIClient.Endpoints field initialization in NewVastAIClient"
      pattern: "c\\.Endpoints = &EndpointService"
    - from: "internal/client/client.go"
      to: "internal/client/worker_groups.go"
      via: "VastAIClient.WorkerGroups field initialization in NewVastAIClient"
      pattern: "c\\.WorkerGroups = &WorkerGroupService"
---

<objective>
Create Go API client services for serverless endpoints and worker groups with typed request/response structs, full CRUD methods, and unit tests using httptest mocks.

Purpose: Provides the foundation layer that all Phase 4 Terraform resources and data sources will call. Follows the exact service sub-object pattern established in Phase 2/3 (client.Instances, client.Volumes, etc.).

Output: Two new client service files with full API coverage, two test files, and updated client.go constructor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-serverless/04-CONTEXT.md
@.planning/phases/04-serverless/04-RESEARCH.md

@internal/client/client.go
@internal/client/network_volumes.go
@internal/client/templates.go
@internal/client/volumes_test.go

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From internal/client/client.go:
```go
type VastAIClient struct {
    httpClient     *retryablehttp.Client
    baseURL        string
    apiKey         string
    userAgent      string
    Instances      *InstanceService
    Offers         *OfferService
    Templates      *TemplateService
    SSHKeys        *SSHKeyService
    Volumes        *VolumeService
    NetworkVolumes *NetworkVolumeService
}

func NewVastAIClient(apiKey, baseURL, version string) *VastAIClient
func (c *VastAIClient) Get(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) Post(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Put(ctx context.Context, path string, body, result interface{}) error
func (c *VastAIClient) Delete(ctx context.Context, path string, result interface{}) error
func (c *VastAIClient) DeleteWithBody(ctx context.Context, path string, body, result interface{}) error
```

From internal/client/errors.go:
```go
type APIError struct {
    StatusCode int
    Message    string
    Method     string
    Path       string
}
```

Pattern from internal/client/network_volumes.go (read-via-list):
```go
type NetworkVolumeService struct { client *VastAIClient }
func (s *NetworkVolumeService) List(ctx context.Context) ([]Volume, error) {
    var resp volumeListResponse
    if err := s.client.Get(ctx, path, &resp); err != nil { ... }
    return resp.Volumes, nil
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint and worker group client services with all API methods</name>
  <files>internal/client/endpoints.go, internal/client/worker_groups.go, internal/client/client.go</files>
  <read_first>
    - internal/client/client.go (VastAIClient struct and constructor to extend)
    - internal/client/network_volumes.go (reference pattern for read-via-list, delete, create-then-read)
    - internal/client/templates.go (reference pattern for DeleteWithBody)
    - .planning/phases/04-serverless/04-RESEARCH.md (API contracts section lines 192-678)
  </read_first>
  <action>
    Create `internal/client/endpoints.go` with EndpointService:

    **Structs (use exact JSON field names from research API contracts):**
    - `EndpointService struct { client *VastAIClient }`
    - `CreateEndpointRequest` with fields: ClientID (json:"client_id"), EndpointName (json:"endpoint_name"), MinLoad (float64, json:"min_load"), MinColdLoad (float64, json:"min_cold_load"), TargetUtil (float64, json:"target_util"), ColdMult (float64, json:"cold_mult"), ColdWorkers (int, json:"cold_workers"), MaxWorkers (int, json:"max_workers"), AutoscalerInstance (string, json:"autoscaler_instance")
    - `UpdateEndpointRequest` with fields: ClientID (string, json:"client_id"), EndptJobID (int, json:"endptjob_id"), EndpointName (string, json:"endpoint_name,omitempty"), MinLoad (*float64, json:"min_load,omitempty"), MinColdLoad (*float64, json:"min_cold_load,omitempty"), TargetUtil (*float64, json:"target_util,omitempty"), ColdMult (*float64, json:"cold_mult,omitempty"), ColdWorkers (*int, json:"cold_workers,omitempty"), MaxWorkers (*int, json:"max_workers,omitempty"), EndpointState (string, json:"endpoint_state,omitempty"), AutoscalerInstance (string, json:"autoscaler_instance")
    - `Endpoint` response struct with fields: ID (int), EndpointName (string), MinLoad (float64), MinColdLoad (float64), TargetUtil (float64), ColdMult (float64), ColdWorkers (int), MaxWorkers (int), EndpointState (string) -- all with json tags
    - `endpointListResponse` (unexported) with Success (bool, json:"success"), Results ([]Endpoint, json:"results"), Message (string, json:"msg,omitempty")

    **Methods:**
    - `Create(ctx, *CreateEndpointRequest) (*Endpoint, error)` -- sets ClientID="me", AutoscalerInstance="prod", POST /endptjobs/, then create-then-read via List, find by name (iterate backwards for most recent match). Return error if not found after create.
    - `List(ctx) ([]Endpoint, error)` -- GET /endptjobs/, check resp.Success, return resp.Results. Same pattern as NetworkVolumeService.List.
    - `Update(ctx, id int, *UpdateEndpointRequest) error` -- sets ClientID="me", EndptJobID=id, AutoscalerInstance="prod", PUT /endptjobs/{id}/
    - `Delete(ctx, id int) error` -- DELETE /endptjobs/{id}/ with body {"client_id":"me","endptjob_id":id} using DeleteWithBody (per Pitfall 2)

    Create `internal/client/worker_groups.go` with WorkerGroupService:

    **Structs:**
    - `WorkerGroupService struct { client *VastAIClient }`
    - `CreateWorkerGroupRequest` with fields: ClientID (string, json:"client_id"), EndpointName (string, json:"endpoint_name,omitempty"), EndpointID (int, json:"endpoint_id,omitempty"), TemplateHash (string, json:"template_hash,omitempty"), TemplateID (int, json:"template_id,omitempty"), SearchParams (string, json:"search_params,omitempty"), LaunchArgs (string, json:"launch_args,omitempty"), GpuRAM (float64, json:"gpu_ram,omitempty"), MinLoad (float64, json:"min_load"), TargetUtil (float64, json:"target_util"), ColdMult (float64, json:"cold_mult"), ColdWorkers (int, json:"cold_workers"), TestWorkers (int, json:"test_workers"), AutoscalerInstance (string, json:"autoscaler_instance")
    - `UpdateWorkerGroupRequest` with fields: ClientID (string, json:"client_id"), AutoJobID (int, json:"autojob_id"), EndpointName (string, json:"endpoint_name,omitempty"), EndpointID (int, json:"endpoint_id,omitempty"), TemplateHash (string, json:"template_hash,omitempty"), TemplateID (int, json:"template_id,omitempty"), SearchParams (string, json:"search_params,omitempty"), LaunchArgs (string, json:"launch_args,omitempty"), GpuRAM (*float64, json:"gpu_ram,omitempty"), MinLoad (*float64, json:"min_load,omitempty"), TargetUtil (*float64, json:"target_util,omitempty"), ColdMult (*float64, json:"cold_mult,omitempty"), ColdWorkers (*int, json:"cold_workers,omitempty"), TestWorkers (*int, json:"test_workers,omitempty")
    - `WorkerGroup` response struct with fields: ID (int), EndpointName (string), EndpointID (int), TemplateHash (string), TemplateID (int), SearchParams (string), LaunchArgs (string), GpuRAM (float64), MinLoad (float64), TargetUtil (float64), ColdMult (float64), ColdWorkers (int), TestWorkers (int)
    - `workerGroupListResponse` (unexported) with Success, Results, Message

    **Methods:**
    - `Create(ctx, *CreateWorkerGroupRequest) (*WorkerGroup, error)` -- sets ClientID="me", AutoscalerInstance="prod", POST /autojobs/, then create-then-read via List, find by highest ID (most recently created). Return error if none found.
    - `List(ctx) ([]WorkerGroup, error)` -- GET /autojobs/, check resp.Success, return resp.Results
    - `Update(ctx, id int, *UpdateWorkerGroupRequest) error` -- sets ClientID="me", AutoJobID=id, PUT /autojobs/{id}/
    - `Delete(ctx, id int) error` -- DELETE /autojobs/{id}/ with body {"client_id":"me","autojob_id":id} using DeleteWithBody

    Update `internal/client/client.go`:
    - Add `Endpoints *EndpointService` and `WorkerGroups *WorkerGroupService` fields to VastAIClient struct
    - Add `c.Endpoints = &EndpointService{client: c}` and `c.WorkerGroups = &WorkerGroupService{client: c}` in NewVastAIClient after existing service initializations
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "EndpointService" internal/client/endpoints.go
    - grep -q "WorkerGroupService" internal/client/worker_groups.go
    - grep -q "Endpoints.*EndpointService" internal/client/client.go
    - grep -q "WorkerGroups.*WorkerGroupService" internal/client/client.go
    - grep -q "c.Endpoints = &EndpointService" internal/client/client.go
    - grep -q "c.WorkerGroups = &WorkerGroupService" internal/client/client.go
    - grep -q "DeleteWithBody" internal/client/endpoints.go
    - grep -q "DeleteWithBody" internal/client/worker_groups.go
    - grep -q '/endptjobs/' internal/client/endpoints.go
    - grep -q '/autojobs/' internal/client/worker_groups.go
    - grep -q "autoscaler_instance" internal/client/endpoints.go
  </acceptance_criteria>
  <done>EndpointService and WorkerGroupService exist with Create/List/Update/Delete methods. VastAIClient initializes both services. Code compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for endpoint and worker group client services</name>
  <files>internal/client/endpoints_test.go, internal/client/worker_groups_test.go</files>
  <read_first>
    - internal/client/endpoints.go (just created in Task 1)
    - internal/client/worker_groups.go (just created in Task 1)
    - internal/client/network_volumes_test.go (reference pattern for httptest mock tests)
    - internal/client/volumes_test.go (additional test pattern reference)
  </read_first>
  <behavior>
    EndpointService tests:
    - TestEndpointService_Create: POST /endptjobs/ with correct body, then GET /endptjobs/ to read back, returns endpoint matching by name
    - TestEndpointService_List: GET /endptjobs/ returns list of endpoints, verifies success check
    - TestEndpointService_Update: PUT /endptjobs/{id}/ with correct body including autoscaler_instance="prod"
    - TestEndpointService_Delete: DELETE /endptjobs/{id}/ with body containing endptjob_id, uses DeleteWithBody

    WorkerGroupService tests:
    - TestWorkerGroupService_Create: POST /autojobs/ with correct body, then GET /autojobs/ to read back, returns worker group with highest ID
    - TestWorkerGroupService_List: GET /autojobs/ returns list of worker groups
    - TestWorkerGroupService_Update: PUT /autojobs/{id}/ with correct body
    - TestWorkerGroupService_Delete: DELETE /autojobs/{id}/ with body containing autojob_id

    All tests verify:
    - Authorization header is "Bearer test-api-key"
    - Request body contains expected JSON fields
    - Response deserialization works correctly
  </behavior>
  <action>
    Create `internal/client/endpoints_test.go` using the httptest mock pattern from existing tests:

    Use the same test setup pattern as `network_volumes_test.go`: create httptest.NewServer, instantiate NewVastAIClient with server URL, call methods, assert results.

    For Create tests: server handler should match POST method on first call (return success), then GET on second call (return list with the created endpoint). Verify both request bodies.

    For List tests: return `{"success": true, "results": [{endpoint data}]}`, verify deserialization.

    For Update tests: verify PUT method, correct path with ID, body contains endptjob_id and autoscaler_instance="prod".

    For Delete tests: verify DELETE method, body contains client_id="me" and endptjob_id.

    Create `internal/client/worker_groups_test.go` with the same pattern:
    - Create test: POST then GET, verify create-then-read finds highest ID
    - List test: verify deserialization of worker group list
    - Update test: verify PUT with autojob_id
    - Delete test: verify DELETE with body containing autojob_id

    Each test file should have 4 tests. Follow the exact assertion style used in existing test files (t.Errorf with descriptive messages, checking specific fields).
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/ -run "TestEndpoint|TestWorkerGroup" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TestEndpointService_Create" internal/client/endpoints_test.go
    - grep -q "TestEndpointService_List" internal/client/endpoints_test.go
    - grep -q "TestEndpointService_Update" internal/client/endpoints_test.go
    - grep -q "TestEndpointService_Delete" internal/client/endpoints_test.go
    - grep -q "TestWorkerGroupService_Create" internal/client/worker_groups_test.go
    - grep -q "TestWorkerGroupService_List" internal/client/worker_groups_test.go
    - grep -q "TestWorkerGroupService_Update" internal/client/worker_groups_test.go
    - grep -q "TestWorkerGroupService_Delete" internal/client/worker_groups_test.go
    - grep -q "Bearer" internal/client/endpoints_test.go
    - grep -q "Bearer" internal/client/worker_groups_test.go
  </acceptance_criteria>
  <done>8 unit tests pass (4 endpoint + 4 worker group). Tests verify correct HTTP methods, paths, request bodies, auth headers, and response deserialization.</done>
</task>

</tasks>

<verification>
```bash
# All client tests pass (existing + new)
cd /home/aescalera/code/ai/infrastructure/vastai && go test ./internal/client/ -v -count=1

# Build succeeds
go build ./...
```
</verification>

<success_criteria>
- EndpointService and WorkerGroupService exist with Create/List/Update/Delete methods
- VastAIClient.Endpoints and VastAIClient.WorkerGroups are initialized in constructor
- 8 new unit tests pass covering all CRUD operations
- All existing tests still pass
- Code compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-serverless/04-01-SUMMARY.md`
</output>
