---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
  - go.mod
  - go.sum
  - internal/provider/provider.go
  - GNUmakefile
  - .golangci.yml
  - terraform-registry-manifest.json
  - examples/provider/provider.tf
  - templates/index.md.tmpl
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - RLSE-03
  - RLSE-04

must_haves:
  truths:
    - "Go module initializes and all dependencies resolve via go mod tidy"
    - "go build ./... compiles the provider binary without errors"
    - "Provider registers with Terraform address registry.terraform.io/realnedsanders/vastai"
    - "Provider schema exposes api_key (sensitive, optional) and api_url (optional) attributes"
    - "terraform-registry-manifest.json declares protocol version 6.0"
  artifacts:
    - path: "main.go"
      provides: "Provider entry point with providerserver.Serve()"
      contains: "providerserver.Serve"
    - path: "go.mod"
      provides: "Go module with all framework dependencies"
      contains: "github.com/realnedsanders/terraform-provider-vastai"
    - path: "internal/provider/provider.go"
      provides: "VastaiProvider struct with Schema and Configure methods"
      exports: ["New", "VastaiProvider"]
    - path: "terraform-registry-manifest.json"
      provides: "Registry protocol declaration"
      contains: "protocol_versions"
    - path: "GNUmakefile"
      provides: "Build, test, lint, install targets"
      contains: "build"
    - path: ".golangci.yml"
      provides: "Linter configuration"
      contains: "linters"
  key_links:
    - from: "main.go"
      to: "internal/provider/provider.go"
      via: "provider.New(version) function call"
      pattern: "provider\\.New"
    - from: "internal/provider/provider.go"
      to: "schema.Schema"
      via: "api_key and api_url attribute definitions"
      pattern: "api_key.*Sensitive.*true"
---

<objective>
Scaffold the Terraform provider Go project: module initialization, provider shell with schema, build tooling, and registry manifest.

Purpose: Create the compilable foundation that all subsequent plans build on -- the Go module, provider entry point, provider struct with schema, build/lint tooling, and registry protocol declaration.
Output: A provider binary that compiles via `go build ./...` and registers at `registry.terraform.io/realnedsanders/vastai` with api_key and api_url schema attributes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and create provider entry point with schema</name>
  <files>main.go, go.mod, go.sum, internal/provider/provider.go</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
1. Initialize Go module:
   ```
   go mod init github.com/realnedsanders/terraform-provider-vastai
   ```
   Set Go version to 1.25 in go.mod (per D-02).

2. Create `main.go` at project root with this exact structure:
   - package main
   - import: context, flag, log, github.com/hashicorp/terraform-plugin-framework/providerserver, github.com/realnedsanders/terraform-provider-vastai/internal/provider
   - var version string = "dev" (ldflags-injectable)
   - func main(): parse --debug flag, create ServeOpts with Address "registry.terraform.io/realnedsanders/vastai" (per D-03), call providerserver.Serve(context.Background(), provider.New(version), opts), log.Fatal on error

3. Create `internal/provider/provider.go`:
   - package provider
   - VastaiProvider struct with `version string` field
   - VastaiProviderModel struct with:
     - APIKey types.String `tfsdk:"api_key"`
     - APIURL types.String `tfsdk:"api_url"`
   - Compile-time interface assertion: var _ provider.Provider = &VastaiProvider{}
   - func New(version string) func() provider.Provider -- returns closure creating VastaiProvider{version: version}
   - func (p *VastaiProvider) Metadata(): set resp.TypeName = "vastai", resp.Version = p.version
   - func (p *VastaiProvider) Schema(): return schema.Schema with Description "Interact with Vast.ai GPU compute infrastructure." and attributes:
     - "api_key": schema.StringAttribute{Description: "Vast.ai API key. Can also be set via VASTAI_API_KEY environment variable.", Optional: true, Sensitive: true} (per FOUND-01, D-09)
     - "api_url": schema.StringAttribute{Description: "Vast.ai API base URL. Can also be set via VASTAI_API_URL environment variable. Defaults to https://console.vast.ai.", Optional: true} (per FOUND-02)
   - func (p *VastaiProvider) Configure(): STUB for now -- parse config into VastaiProviderModel, check IsUnknown() before IsNull() for both fields, read VASTAI_API_KEY and VASTAI_API_URL from environment with config override, error if api_key empty when known, default api_url to "https://console.vast.ai". Do NOT create the client yet (Plan 02 wires the client). Add TODO comment: "// TODO: Create API client and set resp.ResourceData / resp.DataSourceData (Plan 01-02)"
   - func (p *VastaiProvider) Resources(): return empty slice []func() resource.Resource{}
   - func (p *VastaiProvider) DataSources(): return empty slice []func() datasource.DataSource{}
   - Imports needed: context, os, github.com/hashicorp/terraform-plugin-framework/provider, github.com/hashicorp/terraform-plugin-framework/provider/schema, github.com/hashicorp/terraform-plugin-framework/datasource, github.com/hashicorp/terraform-plugin-framework/resource, github.com/hashicorp/terraform-plugin-framework/types

4. Add dependencies:
   ```
   go get github.com/hashicorp/terraform-plugin-framework@v1.19.0
   go get github.com/hashicorp/terraform-plugin-go@v0.31.0
   go get github.com/hashicorp/terraform-plugin-log@v0.10.0
   go mod tidy
   ```

5. Verify compilation: `go build -v ./...`
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && go build -v ./...</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/realnedsanders/terraform-provider-vastai`
    - go.mod contains `go 1.25`
    - go.mod contains `github.com/hashicorp/terraform-plugin-framework v1.19.0`
    - main.go contains `providerserver.Serve`
    - main.go contains `registry.terraform.io/realnedsanders/vastai`
    - main.go contains `var version string = "dev"`
    - main.go contains `provider.New(version)`
    - internal/provider/provider.go contains `var _ provider.Provider = &VastaiProvider{}`
    - internal/provider/provider.go contains `Sensitive:   true` (for api_key)
    - internal/provider/provider.go contains `VASTAI_API_KEY`
    - internal/provider/provider.go contains `VASTAI_API_URL`
    - internal/provider/provider.go contains `https://console.vast.ai`
    - internal/provider/provider.go contains `IsUnknown()`
    - internal/provider/provider.go contains `func New(version string) func() provider.Provider`
    - `go build -v ./...` exits 0
  </acceptance_criteria>
  <done>Go module compiles, provider registers at correct address, schema exposes api_key (sensitive) and api_url attributes, Configure validates env vars with unknown-value safety</done>
</task>

<task type="auto">
  <name>Task 2: Create build tooling, linter config, registry manifest, and scaffolding</name>
  <files>GNUmakefile, .golangci.yml, terraform-registry-manifest.json, examples/provider/provider.tf, templates/index.md.tmpl</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
1. Create `terraform-registry-manifest.json` at project root (per RLSE-03):
   ```json
   {
       "version": 1,
       "metadata": {
           "protocol_versions": ["6.0"]
       }
   }
   ```

2. Create `GNUmakefile` at project root with these exact targets:
   ```makefile
   default: fmt lint install generate

   .PHONY: build install lint fmt test testacc generate

   build:
   	go build -v ./...

   install: build
   	go install -v ./...

   lint:
   	golangci-lint run

   fmt:
   	gofmt -s -w .

   test:
   	go test -v -count=1 -parallel=4 -timeout 120s ./...

   testacc:
   	TF_ACC=1 go test -v -count=1 -parallel=4 -timeout 120m ./...

   generate:
   	go generate ./...
   ```
   IMPORTANT: Use actual tab characters for makefile recipe lines, not spaces.

3. Create `.golangci.yml` at project root with golangci-lint v2 format:
   ```yaml
   version: "2"
   linters:
     default: standard
     enable:
       - staticcheck
       - govet
       - errcheck
       - gosec
       - revive
       - gosimple
       - unconvert
       - unparam
   formatters:
     enable:
       - gofmt
   ```

4. Create `examples/provider/provider.tf`:
   ```hcl
   terraform {
     required_providers {
       vastai = {
         source = "realnedsanders/vastai"
       }
     }
   }

   provider "vastai" {
     # api_key can be set via VASTAI_API_KEY environment variable
     # api_key = "your-api-key-here"

     # api_url defaults to https://console.vast.ai
     # api_url = "https://console.vast.ai"
   }
   ```

5. Create `templates/index.md.tmpl` (tfplugindocs provider overview template):
   ```
   ---
   page_title: "vastai Provider"
   subcategory: ""
   description: |-
     The vastai provider enables Terraform to manage Vast.ai GPU compute infrastructure.
   ---

   # vastai Provider

   The vastai provider enables Terraform to manage Vast.ai GPU compute resources, storage, networking, serverless endpoints, and account configuration.

   ## Authentication

   The provider requires a Vast.ai API key for authentication. You can provide it via:

   1. The `VASTAI_API_KEY` environment variable (recommended)
   2. The `api_key` attribute in the provider configuration block

   ## Example Usage

   {{ tffile "examples/provider/provider.tf" }}

   {{ .SchemaMarkdown | trimspace }}
   ```

6. Verify: `go build -v ./...` still compiles, `make build` works.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/infrastructure/vastai && make build</automated>
  </verify>
  <acceptance_criteria>
    - terraform-registry-manifest.json contains `"protocol_versions": ["6.0"]`
    - terraform-registry-manifest.json contains `"version": 1`
    - GNUmakefile contains `go build -v`
    - GNUmakefile contains `golangci-lint run`
    - GNUmakefile contains `TF_ACC=1`
    - GNUmakefile contains `go test -v -count=1`
    - .golangci.yml contains `version: "2"`
    - .golangci.yml contains `staticcheck`
    - .golangci.yml contains `errcheck`
    - .golangci.yml contains `gosec`
    - examples/provider/provider.tf contains `source = "realnedsanders/vastai"`
    - templates/index.md.tmpl contains `VASTAI_API_KEY`
    - `make build` exits 0
  </acceptance_criteria>
  <done>Registry manifest declares protocol 6.0, GNUmakefile has all standard targets, golangci-lint v2 config with required linters, provider example and doc template exist</done>
</task>

</tasks>

<verification>
- `go build -v ./...` compiles without errors
- `make build` succeeds
- `grep -r "Sensitive.*true" internal/provider/provider.go` finds the api_key sensitive flag
- `grep "protocol_versions" terraform-registry-manifest.json` confirms protocol 6.0
- `cat go.mod | grep "terraform-plugin-framework v1.19.0"` confirms framework version
</verification>

<success_criteria>
- Provider binary compiles from source
- Provider registers at registry.terraform.io/realnedsanders/vastai
- Provider schema has api_key (sensitive, optional) and api_url (optional) attributes
- Configure method handles unknown values, env var fallback, and missing key errors
- Registry manifest declares protocol 6.0
- Build tooling (make, lint, fmt) operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
