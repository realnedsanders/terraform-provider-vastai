version: "2"
linters:
  default: standard
  enable:
    - staticcheck
    - govet
    - errcheck
    - gosec
    - revive
    - gosimple
    - unconvert
    - unparam
formatters:
  enable:
    - gofmt
